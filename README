A simple and inefficient standalone implementation of the *NIX cksum command in Nim. Not pretty code. Loads everything into memory, so you probably don't want to use it with big files if your system can't handle it. It's also very slow, at least compared to my system's implementation. 